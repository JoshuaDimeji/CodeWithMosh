import math

x = 3.2
print(round(x)) # this round up to the whole number

print(math.ceil(x)) # just like round() but it's different. It rounds to the upper part
print(math.floor(x)) # just the opposite of ceil(). It approx. to the lower part
print(math.trunc(x))

y = 5
print(math.factorial(y))